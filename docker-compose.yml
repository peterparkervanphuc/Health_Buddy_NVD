version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: healthbuddy
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: healthbuddy_db
    ports:
      - "5432:5432"

  redis:
    image: redis:7
    ports:
      - "6379:6379"